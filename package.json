{
  "name": "cognito-client",
  "version": "1.0.0",
  "description": "Generic, dependency-injected browser Cognito client",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "postinstall": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
